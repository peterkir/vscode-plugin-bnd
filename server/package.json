{
    "name": "vscode-bnd-server",
    "description": "Bnd language server",
    "version": "0.2.0",
    "private": true,
    "scripts": {
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./"
    },
    "dependencies": {
        "vscode-languageserver": "^9.0.1",
        "vscode-languageserver-textdocument": "^1.0.12"
    },
    "devDependencies": {
        "typescript": "^6.0.3"
    }
}
